/** @type {import('next').NextConfig} */
const nextConfig = {
  // Adicione aqui futuras configurações do Next.js, se necessário.
  // Por exemplo: reactStrictMode: true,
};

module.exports = nextConfig;